/*! progressify v0.3.0 | (c) 2015 Andrew Carlson  */
!function(s){s.fn.progressify=function(o){if("destroy"===o)/*
			Removes the scroll event handler and the progress bar if it was added.
			*/
return s(window).off("scroll",h),s(".progressify").remove(),this;/*
			The article or post on which the progress is to be calculated.
			*/
var r=this;if(r.length){var i=r.outerHeight(),t=r.offset().top,e=".progressify-value",a=".progressify-bar",n=function(s,o,r){var i;return function(){var t=this,e=arguments,a=function(){i=null,r||s.apply(t,e)},n=r&&!i;clearTimeout(i),i=setTimeout(a,o),n&&s.apply(t,e)}},o=o||{};/*
				The element that functions as the progress bar wrapper.
				*/
o.progressWrapper="string"==typeof o.progressWrapper?o.progressWrapper:a,/*
				The element that functions as a progress bar. 
				This is the element that receives the completion percentage.
				*/
o.progressBar="string"==typeof o.progressBar?o.progressBar:e,/*
				The amount of time in milliseconds to wait before calling the function again.
				This is to prevent the browser from using up too many resources by calling the scroll event non-stop.
				*/
o.debounceTime=isNaN(o.debounceTime)?100:o.debounceTime,/*
				Optional position of the progress bar. 
				Accepts: 'left' || 'top' || 'bottom' || 'right'
				If a position is passed, the bar will receive a fixed position.
				If the default progress bar is used, the bar will receive a fixed bottom position.
				Otherwise no position will be applied.
				*/
void 0===o.position?o.progressBar===e&&(o.position="bottom"):o.position.match(/^(top|bottom|left|right)$/)&&(o.position=o.position),/*
				height or width depending on position.
				*/
void 0!==o.position&&(o.position.match(/^(top|bottom)$/)?o.orientation="width":o.position.match(/^(left|right)$/)&&(o.orientation="height")),/*
				Adds optional CSS classes to progress wrapper.
				*/
o.cssClass="string"==typeof o.cssClass?o.cssClass:"",/*
				Optionally force the data-progress attribute
				*/
o.showData=o.showData?o.showData:void 0,/*
				Optionally show the progress as a percentage
				*/
o.showProgress=o.showProgress?o.showProgress:void 0,/*
				Percentage completed element
				*/
o.showProgressCustom="string"==typeof o.showProgressCustom?o.showProgressCustom:!1,/*
				Percentage completed element placement
				*/
o.showProgressPosition=void 0!==o.showProgressPosition&&o.showProgressPosition.match(/^(inside|outside)$/)?o.showProgressPosition:"outside",/*
				If options.progressBar is the default option, create the progress element.
				*/
o.progressBar===e&&r.append('<div class="progressify-bar"><div class="progressify-value"></div></div>');/*
				Add optional CSS classes as well as the position class to the element
				*/
var p=s(o.progressBar);/*
				If a percentage of progress is desired AND the user didn't specify a custom percentage element:
				Add the percentage after the progress bar.
				*/
o.showProgress!==!0||o.showProgressCustom||($showProgEl='<span class="progressify-progress"></span>',"inside"===o.showProgressPosition?p.append($showProgEl):p.after($showProgEl));var g=o.cssClass;void 0!==o.position&&(g+=" progressify progressify-position-"+o.position),s(o.progressWrapper).addClass(g);/*
				Actual progression function
				*/
var h=n(function(o){var r=(o.data.$article,o.data.options),e=s(window).scrollTop()-t,a=e/i>=0?e/i:0;a=101>100*a?Math.floor(100*a):100,a+="%";var n=s(r.progressBar);/*
					Update the progress completion number if showProgress === true
					*/
if(/*
					If the orientation === undefined OR if showData === true apply a data attribute with the progress
					*/
(void 0===r.orientation||r.showData===!0)&&n.attr("data-progress",a),r.showProgress===!0){var p=r.showProgressCustom?r.showProgressCustom:".progressify-progress";s(p).html(a)}/*
					If the orientation (width | height) has been defined, set the completion as a percentage of the orientation.
					*/
void 0!==r.orientation&&n.css(r.orientation,a)},o.debounceTime);/*
				Attach the event handler to scroll
				*/
s(window).on("scroll",{$article:r,options:o},h)}/*
			Return the original object to the jQuery chain
			*/
return r}}(jQuery);